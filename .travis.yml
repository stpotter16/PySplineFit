sudo: required
language: python

services:
  - docker

before_install:
  - docker build -t pysplinefit .

script:
  - docker run pysplinefit /bin/bash -c "cd /code && source activate test && python -m pytest -v"

notifications:
  email: false